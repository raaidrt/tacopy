# Project Guidelines
We are using the `uv` Python package manager for this project. When installing dependencies, please add them using `uv add`. 

When running python, please activate the virtual environment provided by `uv`. 